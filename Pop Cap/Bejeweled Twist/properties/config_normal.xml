<Properties>
	<!-- Number of rows, columns in game board  -->
	<Integer id="GameRows">	8		</Integer>
	<Integer id="GameColumns">	8		</Integer>

  
  
  <!--              COUNTER GEMS                 -->
  <!-- what is the first level they appear on? -->
  <Integer id="CounterFirstLevel"> 2 </Integer>
  
  <!-- what is the value for that first level -->
  <Integer id="CounterBase"> 35 </Integer>
  
  <!-- creates an increasing probability instead of an event -->
  <Double id="CounterChancePow"> 5.0 </Double>
  
  <!-- does this value decrement based on turns or gems? -->
  <Boolean id="CounterTurnBased"> false </Boolean>
  
  <!-- how many level ups until the value changes -->
  <Integer id="CounterLevelsBetweenChange" > 1 </Integer>
  
  <!-- what is the change to the value? -->
  <Integer id="CounterChange"> -5 </Integer>
  
  <!-- what level does this value start to change? -->
  <!-- Integer id="CounterFirstChangeLevel"> 2 </Integer -->
  
  <!-- what is the minimum the value can be? -->
  <Integer id="CounterMin"> 7 </Integer>      
  
  
  <!-- what is the value for that first level -->
  <Integer id="CounterValueBase"> 20 </Integer>
  
  <!-- what is the change to the value? -->
  <Integer id="CounterValueChange"> -1 </Integer>
  
  <!-- what is the minimum the value can be? -->
  <Integer id="CounterValueMin"> 7 </Integer>
  
  <Integer id="CounterValueMax"> 20 </Integer>


  

  <!--                BAD GEMS              -->
  <!-- how many "bad" gems (skulls, devils, counters, locks) can be on the board at once? -->

  <!-- what is the first level they appear on? -->
  <Integer id="BadGemsOnBoardMaxFirstLevel"> 1 </Integer>
  
  <!-- what is the value for that first level -->
  <Integer id="BadGemsOnBoardMaxBase"> 3 </Integer>
  
  <!-- what is the change to the value? -->
  <Integer id="BadGemsOnBoardMaxChange"> 1 </Integer>
  
  <!-- how many level ups until the value changes -->
  <Integer id="BadGemsOnBoardMaxLevelsBetweenChange" > 3 </Integer>
  
  <!-- what level does this value start to change? -->
  <Integer id="BadGemsOnBoardMaxFirstChangeLevel"> 6 </Integer>

  <!-- what is the maximum the value can be? -->
  <Integer id="BadGemsOnBoardMaxMax"> 15 </Integer>
    


  <!-- what is the first level they appear on? -->
  <Integer id="LevelUpPointsFirstLevel"> 1 </Integer>

  <!-- what is the value for that first level -->
  <Integer id="LevelUpPointsBase"> 2000 </Integer>

  <!-- what is the change to the value? -->
  <Integer id="LevelUpPointsChange">650 </Integer>




  <!-- gem combo size required to get "amnesty" on bad gem appearing next move -->
  <Integer id="AmnestyGemCount"> 8 </Integer>

  
  <!--               LOCK GEMS                      -->
  <!-- what is the first level they appear on? -->
  
  <Integer id="LockGemFirstLevel"> 4 </Integer>
  
  <!-- what is the value for that first level -->
  <Integer id="LockGemBase"> 35 </Integer>

  <Double id="LockGemChancePow"> 6.0 </Double>
  
  <!-- does this value decrement based on turns or gems? -->
  <Boolean id="LockGemTurnBased"> true </Boolean>
  
  <!-- how many level ups until the value changes -->
  <Integer id="LockGemLevelsBetweenChange" > 1 </Integer>
  
  <!-- what is the change to the value? -->
  <Integer id="LockGemChange"> -3 </Integer>
  
  <!-- what is the minimum the value can be? -->
  <Integer id="LockGemMin"> 7 </Integer>
  
  
  <!--                  SKULL GEMS                   -->
  
  <!-- what is the first level they appear on? -->
  <Integer id="SkullsFirstLevel"> 25 </Integer>
  
  <!-- what is the value for that first level -->
  <Integer id="SkullsBase"> 26 </Integer>
  
  <!-- does this value decrement based on turns(true)  or gems (false) ? -->
  <Boolean id="SkullsTurnBased"> true </Boolean>
  
  <!-- how many level ups until the value changes -->
  <Integer id="SkullsLevelsBetweenChange"> 1 </Integer>
  
  <!-- what is the change to the value? -->
  <Integer id="SkullsChange"> -1 </Integer>
  
  <!-- what is the minimum the value can be? -->
  <Integer id="SkullsMin"> 10 </Integer>



  <!--             DEVIL GEMS               -->
  <!-- what is the first level they appear on? -->
  <Integer id="DevilFirstLevel"> 13 </Integer>

  <!-- what is the value for that first level -->
  <Integer id="DevilBase"> 100 </Integer>

  <!-- what is the change to the value? -->
  <Integer id="DevilChange"> -1 </Integer>

  <!-- does this value decrement based on turns or gems? -->
  <Boolean id="DevilTurnBased"> false </Boolean>

  <!-- how many level ups until the value changes -->
  <Integer id="DevilLevelsBetweenChange" > 1 </Integer>

  <!-- what is the minimum the value can be? -->
  <Integer id="DevilMin"> 20 </Integer>


  <!-- what is the value for that first level -->
  <Integer id="DevilValueBase"> 13 </Integer>

  <!-- what is the change to the value? -->
  <Integer id="DevilValueChange"> -3 </Integer>

  <!-- what is the minimum the value can be? -->
  <Integer id="DevilValueMin"> 9 </Integer>



  <!--               Coal GEMS                      -->
  <!-- what is the first level they appear on? -->
  <Integer id="CoalGemFirstLevel"> 3 </Integer>

  <!-- what is the value for that first level -->
  <Integer id="CoalGemBase"> 100 </Integer>

  <Double id="CoalGemChancePow"> 3.5 </Double>

  <!-- does this value decrement based on turns or gems? -->
  <Boolean id="CoalGemTurnBased"> false </Boolean>

  <!-- how many level ups until the value changes -->
  <Integer id="CoalGemLevelsBetweenChange" > 3 </Integer>

  <!-- what is the change to the value? -->
  <Integer id="CoalGemChange"> -2 </Integer>

  <!-- what is the minimum the value can be? -->
  <Integer id="CoalGemMin"> 35 </Integer>


  <Integer id="FruitFirstLevel"> 8 </Integer>

  <!--  overall multiplier of points for each move based on what game you're playing -->
  <Integer id="ModeMultiplier"> 2 </Integer>


</Properties>
